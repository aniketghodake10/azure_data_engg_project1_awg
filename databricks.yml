bundle:
  name: dabtutorial

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
# variables:
#   catalog:
#     description: The catalog to use
#   schema:
#     description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405616918812103.3.azuredatabricks.net
    # variables:
    #   catalog: dabtutorial_cata
    #   schema: ${workspace.current_user.short_name}
  # prod:
  #   mode: production
  #   workspace:
  #     host: https://adb-7405607237575633.13.azuredatabricks.net
  #     # We explicitly deploy to /Workspace/Users/dellcorei9nvidia@outlook.com to make sure we only have a single copy.
  #     root_path: /Workspace/Users/dellcorei9nvidia@outlook.com/.bundle/${bundle.name}/${bundle.target}
  #   variables:
  #     catalog: dabtutorial_cata
  #     schema: prod
  #   permissions:
  #     - user_name: dellcorei9nvidia@outlook.com
  #       level: CAN_MANAGE